// Profile management is exposed via /api/v1/users/me/profile in the users module.
// This stub exists for future public profile pages (e.g., /api/v1/profiles/:username).
// TODO: Add public profile endpoint with selected watch/favorite data if user opts in.
export {};
